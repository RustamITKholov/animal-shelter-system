package animalSystem.animals;

public class PackAnimals extends Animal {
    public PackAnimals(String type, String name, String birthDate, String[] commands) {
        super(type, name, birthDate, commands);
    }
}
